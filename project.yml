name: Voce
options:
  deploymentTarget:
    macOS: "13.0"
packages:
  VoceKit:
    path: ./VoceKit
  Moonshine:
    url: https://github.com/moonshine-ai/moonshine-swift.git
    from: 0.0.49
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle.git
    from: 2.6.4
targets:
  Voce:
    type: application
    platform: macOS
    sources:
      - path: Voce
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.voceapp.voce
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_IDENTITY: Apple Development
        CODE_SIGN_ENTITLEMENTS: Voce/Voce.entitlements
        SWIFT_VERSION: 6.0
        MARKETING_VERSION: 0.4.8
        CURRENT_PROJECT_VERSION: 11
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Voce/Info.plist
      configs:
        Release:
          ENABLE_HARDENED_RUNTIME: YES
    dependencies:
      - package: VoceKit
        product: VoceKit
      - package: Moonshine
        product: Moonshine
      - package: Sparkle
        product: Sparkle
